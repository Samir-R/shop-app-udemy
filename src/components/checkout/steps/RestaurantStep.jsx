import React from 'react';
import ShopAndShipping from '../../shop/shop-shipping.component';

const RestaurantStep = () => {
  return <ShopAndShipping mode="checkout" />;
};

export default RestaurantStep;
